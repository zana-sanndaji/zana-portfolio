// src/app/api/views/route.js
// این نسخه بدون دیتابیس کار می‌کنه — فقط یه عدد ثابت برمی‌گردونه (ولی بعداً می‌تونی واقعی کنی)

export async function GET() {
  // اینجا بعداً می‌تونی با Vercel KV یا Supabase واقعی کنی
  // الان فقط یه عدد خفن برمی‌گردونیم
  const fakeViews = Math.floor(Math.random() * 800) + 4200; // بین 4200 تا 5000

  return Response.json({ views: fakeViews.toLocaleString() + "+" });
}
